// @ts-nocheck
const{checkPassword:checkPassword,setSessionCookie:setSessionCookie,checkLoginRateLimit:checkLoginRateLimit,resetLoginRateLimit:resetLoginRateLimit}=require("./_lib/auth");module.exports=async function(e,t){if(t.setHeader("Cache-Control","no-store"),"POST"!==e.method)return t.setHeader("Allow",["POST"]),void t.status(405).json({error:"Method not allowed"});if(!process.env.ADMIN_PASSWORD)return void t.status(500).json({error:"ADMIN_PASSWORD is not set on the server"});const o=e.headers["x-forwarded-for"]?.split(",")[0].trim()||"unknown";if(!await checkLoginRateLimit(o))return void t.status(429).json({error:"Too many attempts. Try again later."});let s=e.body;if("string"==typeof s)try{s=JSON.parse(s)}catch{s={}}const r=s&&s.password;checkPassword(r)?(await resetLoginRateLimit(o),setSessionCookie(t),t.status(200).json({ok:!0})):t.status(401).json({error:"Incorrect password"})};